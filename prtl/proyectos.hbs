<section class="cards">
  {{#each proyectos}}
    <div class="project1">
      <img
        src="{{imgUrl}}"
        alt="Página de Biografía Personal"
        class="imgbp"
        width="300px"
        height="300px"
      />
      <h2 class="pt1">{{name}}</h2>
      <p class="pd1">{{content}}</p>
    </div>

  {{/each}}
</section>